# MoltBot Fly.io Environment Variables
# Copy this file and set your values, or use `fly secrets set` directly

# ============================================================================
# Required
# ============================================================================

# Anthropic API key (get from https://console.anthropic.com/settings/keys)
ANTHROPIC_API_KEY=sk-ant-api03-...

# Gateway authentication token (generate with: openssl rand -hex 32)
# Anyone with this token can pair devices and access your bot
CLAWDBOT_GATEWAY_TOKEN=your-64-char-hex-token

# ============================================================================
# Optional: Additional AI Providers
# ============================================================================

# OpenAI (for GPT models)
# OPENAI_API_KEY=sk-...

# Google AI (for Gemini models)
# GOOGLE_API_KEY=...

# ============================================================================
# Optional: Channel Tokens
# ============================================================================

# Discord bot token (get from Discord Developer Portal)
# DISCORD_BOT_TOKEN=MTQ...

# Telegram bot token (get from @BotFather)
# TELEGRAM_BOT_TOKEN=...

# ============================================================================
# Notes
# ============================================================================

# Set secrets in Fly.io with:
#   fly secrets set KEY=value --app your-app-name
#
# View current secrets (names only):
#   fly secrets list --app your-app-name
#
# Prefer environment variables over config file for API keys and tokens.
# This keeps secrets out of moltbot.json where they could be exposed.
